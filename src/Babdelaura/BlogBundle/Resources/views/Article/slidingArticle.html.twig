{# default Sliding pagination control implementation #}

{% if pageCount > 1 %}

    {% if next is defined %}

            <a class="bab-Button" href="{{ path(route, query|merge({(pageParameterName): next})) }}"><i class="fa fa-chevron-circle-left"></i> Articles plus anciens</a>

    {% endif %}


    {% if previous is defined %}

            <a class="bab-Button pull-right" href="{{ path(route, query|merge({(pageParameterName): previous})) }}">Articles plus récents <i class="fa fa-chevron-circle-right"></i></a>

    {% endif %}

{% endif %}
